# Orchestrator Service

FastAPI application that manages personas, voices, scripts, and render jobs. Includes LangChain agent tooling, policy enforcement, JWT auth, and Kafka routing for the render pipeline.
